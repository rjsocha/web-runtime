services:
  web:
    image: wyga/web-runtime-dotnet:latest
    ports:
      - "8001:8080"
    deploy:
      replicas: 4
      placement:
        constraints:
          - node.labels.OS == LINUX
      restart_policy:
        condition: any
      update_config:
        order: start-first
