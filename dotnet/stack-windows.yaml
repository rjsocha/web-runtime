services:
  web:
    image: wyga/web-runtime-dotnet:latest
    ports:
      - "8082:8080"
    deploy:
      replicas: 4
      placement:
        constraints:
          - node.labels.OS == WINDOWS
      restart_policy:
        condition: any
      update_config:
        order: start-first
