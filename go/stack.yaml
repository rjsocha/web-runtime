services:
  web:
    image: wyga/web-runtime-go:latest
    ports:
      - "8010:8080"
    deploy:
      replicas: 4
      restart_policy:
        condition: any
      update_config:
        order: start-first
