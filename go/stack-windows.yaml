services:
  web:
    image: wyga/web-runtime-go:latest
    ports:
      - "8012:8080"
    deploy:
      replicas: 4
      placement:
        constraints:
          - node.labels.OS == WINDOWS
      restart_policy:
        condition: any
      update_config:
        order: start-first
